/**
 * Onboarding layout — full-screen, no split panel.
 * Each onboarding page renders its own background and layout.
 */
export default function OnboardingLayout({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}
