import { UsersThree } from "@phosphor-icons/react/dist/ssr";
import { ComingSoon } from "../_components/ComingSoon";

export default function OrganizerAttendeesPage() {
  return (
    <ComingSoon
      icon={UsersThree}
      title="Attendee management"
      description="See everyone attending your events in one place — contact details, ticket tier, check-in status, and purchase history across all your events."
    />
  );
}
